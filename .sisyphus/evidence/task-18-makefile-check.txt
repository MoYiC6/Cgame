Task 18 evidence: backend/Makefile command entry expansion

Validation commands:
- `cd backend && make -n sqlc-generate`
  - Output: `sqlc generate -f sqlc.yaml`
- `cd backend && make -n migrate-up`
  - Output:
    - `: "${DB_DSN:?未设置 DB_DSN，无法执行 migrate-up}"`
    - `goose -dir migrations postgres "${DB_DSN}" up`
- `cd backend && make -n migrate-down`
  - Output:
    - `: "${DB_DSN:?未设置 DB_DSN，无法执行 migrate-down}"`
    - `goose -dir migrations postgres "${DB_DSN}" down`
- `cd backend && make test-integration -n`
  - Output: `go test ./test/integration`

Extra check:
- `lsp_diagnostics` on `backend/Makefile` could not run because no LSP server is configured for Makefile extension in this workspace.
